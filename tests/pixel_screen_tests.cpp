#include "doctest.h"

#include "../wrapper.h"
using namespace cvtsw;


TEST_CASE("get_line_height")
{
   //constexpr int unimportant = 1;
   //CHECK_EQ(pixel_screen{ unimportant, 1, unimportant, 0, color{} }.get_line_height(), 1);
   //CHECK_EQ(pixel_screen{ unimportant, 2, unimportant, 0, color{} }.get_line_height(), 1);
   //CHECK_EQ(pixel_screen{ unimportant, 3, unimportant, 0, color{} }.get_line_height(), 2);
   //CHECK_EQ(pixel_screen{ unimportant, 4, unimportant, 0, color{} }.get_line_height(), 2);

   //CHECK_EQ(pixel_screen{ unimportant, 1, unimportant, 1, color{} }.get_line_height(), 1);
   //CHECK_EQ(pixel_screen{ unimportant, 2, unimportant, 1, color{} }.get_line_height(), 2);
   //CHECK_EQ(pixel_screen{ unimportant, 3, unimportant, 1, color{} }.get_line_height(), 2);
}

